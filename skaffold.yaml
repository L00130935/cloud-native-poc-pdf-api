apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/l00130935/cloud-native-poc-pdf-api:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/l00130935/cloud-native-poc-pdf-api:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: cloud-native-poc-pdf-api
        chartPath: charts/cloud-native-poc-pdf-api
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/l00130935/cloud-native-poc-pdf-api"
          image.tag: "{{.DIGEST_HEX}}"
